# class Node, working on
class Node:
  def __init__(self, data, right, left):
    self.data = data
    self.right = right;
    self.left = left;
test = Node(10, 0, 0)

print(test.data)
print(test.right)
print(test.left)